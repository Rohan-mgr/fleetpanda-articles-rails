
<h1 class="text-center mt-3 me-auto">Articles</h1>

<p class="text-center">Our articles has <%= Article.public_count %> articles and counting!</p>
<div class="d-flex justify-content-end pe-5">
    <%= link_to "+ New Article", new_article_path,  class: "btn btn-success text-center fw-bold fs-6"%>     
</div>
<div class="d-flex justify-content-center align-items-center flex-wrap px-5 gap-5 pb-5">
     <% @articles.each do |article| %>
        <% unless article.archived? %>
            
            <div class="card" style="width: 20rem;">
                <img src="https://www.shutterstock.com/image-vector/financial-news-trading-stock-impulses-600nw-2281259029.jpg" class="card-img-top" alt="new_article" />
                <div class="card-body">
                  <h5 class="card-title"><%= article.title.capitalize[0, 45] %><%= article.title.length > 45 ? "...":"" %></h5>
                  <p class="card-text">
                    <%= article.body[0, 120] %>
                        <% if article.body.length > 120 %>
                          <%= link_to "Read More...", article, class: "text-decoration-none" %>
                        <% end %>
                  </p>
                  <%= link_to "View More", article, class: "btn btn-primary" %>
                </div>
            </div>
        <% end %>
    <% end %>
</div>
<%# can also be new_article_url %>